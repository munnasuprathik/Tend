# Ingress for external access (if using ingress controller)
# Update with your domain and TLS certificate
#
# IMPORTANT: Replace YOUR_API_DOMAIN with your actual API domain
# Example: api.yourdomain.com or backend.yourdomain.com

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tend-backend-ingress
  annotations:
    # For NGINX Ingress Controller
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    # For cert-manager (automatic TLS)
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - YOUR_API_DOMAIN  # Replace with your actual API domain
    secretName: tend-backend-tls
  rules:
  - host: YOUR_API_DOMAIN  # Replace with your actual API domain
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: tend-backend-service
            port:
              number: 80

