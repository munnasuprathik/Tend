{
  "name": "tend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm start",
    "start": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "test": "cd frontend && npm test",
    "backend": "python -m uvicorn backend.server:app --reload --host 0.0.0.0 --port 8000"
  }
}

